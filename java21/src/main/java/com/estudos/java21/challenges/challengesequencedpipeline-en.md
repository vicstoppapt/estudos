**PT:** [challengesequencedpipeline.md](challengesequencedpipeline.md)

# `ChallengeSequencedPipeline`

Solution: `ChallengeSequencedPipeline.java`.

## Goal

Iterate with **`reversed()`** on `SequencedCollection` — the sum illustrates reverse traversal (same numeric total as forward order for a simple sum).

## Nuances

- New API on ordered collections; contrast with `Collections.reverse`, which mutates or copies.
